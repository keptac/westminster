import {
  Card,
  CardHeader,
  CardContent,
  Divider
} from '@material-ui/core';

const ChatArea = (props) => (
  <Card {...props}>
    <CardHeader
      title="Chat with kelvin"
    />
    <Divider />
    <CardContent>
      COOMING SOON
    </CardContent>

  </Card>
);

export default ChatArea;
